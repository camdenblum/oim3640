## What I learned today

If terminal app is boring try to ask AI to add colors using rich or any other library that would be good.

Review:
stocks = 'AAPL, MSFT, GOOG, AMZNAAPL, MSFT, GOOG, AMZN' --> this is all a string

    stocks [0]--> means first character in the string?, stocks [-1] --> last character 
        A, N

    how to you make it become 'AAPL, MSFT, GOOG, AMZN, TSLA'?

       stocks = stocks + ',TSLA'

    What is "GOOG" in stocks? "AA" in stocks 
    True, True

    stocks.islower()
    False

    stocks.lower
    'appl, nsft, goog, amzn, tsla'

    stocks.find('MSFT')

    'BABSON'.find('B') --> returns the firt B
        0
    Sort stocks in reverse?

    stocks = 'AAPL, MSFT, GOOG, AMZN,TSLA'
    list = [s.strip() for s in stocks.split(',')]
    sorted_stocks = sorted(list, reverse=True)
    print(sorted_stocks)
    print(sorted(set(list), reverse=True))

    print(sorted_stocks)
    in operator in string is differnt than in operator in list

    stocks.strip('A')? --> removes begging and ending A 
    'PL, MSFT,GOOG, AMZN, TSL'

IN  A TEXT FILE THERE IS ALWAYS AN INVISABLE \n --> start in a new line 
    to fix this use a.strip()

def count_vowels(s)
    count = 0
    for c in s:
        if c in 'aeiou':
            count += 1 
            return count --> stops the loop right away
        return count
print(count_vowels('apple')) --> 1
print(count_vowels('ski')) --> 0
print(count_vowels('ski')) --> 1

You can use lists to store multiple values! :)
   Maybe can use lists to store card values like A, K, Q, ect.. then randomize that

stocks = ['AAPL', 'GOOG', 'MSFT', 'AMZN']
'A' in stocks[0]
True

for stock in stocks:
    if 'A' in stock:
        print(stock)
AAPL
AMZN

for stock in stocks:
    if 'AA' in stocks:
        print(stock)
AAPL

stocks_info = [['AAPL', 100, 'Hardware'], ['MSFT', 300, SOFTWARE']]
import pprint
pprint.pprint(stocks_info)
[['AAPL', 100, 'Hardware'], ['MSFT', 300, SOFTWARE']]

for stock in stocks_info 
    if "Hardware" in stock:
        print(stock[0])
AAPL



## Code/work I'm proud of (optional)

## Challenges I faced

## Questions

##AI Usage

This was the prompt I used to refine the blackjack area of the miniprojet00 
I am trying to make an app where a user can play blackjack, take this code and allow it to print the type of card along wth the total vlaue of the card, use tradistional card values for te game and allow for the user to use the ace as a 1 or 11. Make the app look visually nice using rich library and slowly printing each printed line of text so it just doenst show up. Make the code include all of this so it is not one big while loop but alos use basic code so a begginer as myself can understand it. 
#Blackjack Game ##USE RICH LIBRARY

import random

playing = True

if choice == "blackjack":

#while loops allows the user to play as many hands as they like until they want to stop. Randit allows for random card selection.
    while playing:

        
        #allows user to place a bet
        choice_blackjack_bet = input("how much would you like to bet?") 
        blackjack_bet = int(choice_blackjack_bet)
        print("Your remaining balance is $", balance - blackjack_bet)

        player_card = random.randint(1,11)
        dealer_card = random.randint(1,11)

        print("You drew:", player_card)
        print("Dealer drew:", dealer_card)
        player_card = player_card + random.randint(1,11)
        print("You drew:", player_card)
        if player_card == 21:
                print("Blackjack!")
                print("You won $", blackjack_bet*2)
                balance += blackjack_bet * 2
                print("Your remaining balance is $", balance)
                break
        #Note: we do not print dealers second card as in the rules the player does not see it
        dealer_card = random.randint(1, 11) + dealer_card
      
#allows the user to make a decision on how they want to play, along with the functions that determine if they win or lose
        choice = input("Would you like to hit or stand?: ")
        while choice == "hit":
            player_card = player_card + random.randint(1,11)
            print("You drew:", player_card)
            if player_card > 21:
                print("you bust!")
                print("Your remaining balance is $", balance - blackjack_bet)
                balance -= blackjack_bet
                print("Your remaining balance is $", balance)
                break
            choice = input("Would you like to hit or stand?: ")

        if choice == "stand":
            print("Dealer has:", dealer_card)
            while dealer_card < 17:
                dealer_card = dealer_card + random.randint(1,11)
                print("Dealer drew:", dealer_card)

        if player_card > dealer_card and player_card <= 21:
            print("You win!")
            print("You won $", blackjack_bet*2)
            balance += blackjack_bet 
            print("Your remaining balance is $", balance)

        elif dealer_card > 21:
            print("You win!")
            print("You won $", blackjack_bet*2)
            balance += blackjack_bet 
            print("Your remaining balance is $", balance)
        elif player_card < dealer_card:
            print("You lose")
            print("Your remaining balance is $", balance - blackjack_bet)
            balance -= blackjack_bet
            print("Your remaining balance is $", balance)

#allows the user to play as many hands as they like until they want to stop.
        choice = input("Play another hand? (yes/no): ")
        if choice == "no":
            playing = False
        if choice == "yes":
            continue
        else:
            print("Thank's for playing!")
