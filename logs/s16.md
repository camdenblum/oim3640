##What I learned today

Proposal for mini project 3 due end of week

To know what keys are there, you can use the keys methon - print(info.keys())

CompanyOfficers in s16.py is a list of dictionaries
    This is good because we can find information on a specific officer
    Required to share this information on one person and it should have some certian attribuites contained in their specific dictionary 
    A person needs this infomration so its better do design in this way

Review Question: 

info = yf.Ticker('AAPL').info
# 'shortName', 'city', 'longBusinessSummary',
# 'sector', 'fullTimeEmployees', ..
tickers = ['AAPL', 'NVDA', 'MSFT']
prices = {}
for t in tickers: #this for loop iteates through the dictionary and stores each value within prices
    prices[t] = yf.Ticker(t).info['currentPrice']

What does sorted(tickers) return? Does it change tickers?
    sorted(tickers) does not return --> sorted(prices.values) returns 

How do you get the total value of all 3 stocks?
What does 'TSLA' in prices return?
How would you add 'GOOG' to both tickers and prices?
    append

tickers.append('GOOG')
print(tickers)
for t in tickers:
    prices[t] = yf.Ticker(t).info['currentPrice']
print(prices)

Explore: pull other fields and build a richer dict!


stocks = {} #{'NVDA': [open, currentPrice, volume]}
for t in tickers:
    stocks[t] = [yf.Ticker(t).info['open'], yf.Ticker(t).info['currentPrice'], yf.Ticker(t).info['volume']]
print(stocks)


stocks = {} #{'NVDA': [open, currentPrice, volume]}
for t in tickers:
    stocks[t] = yf.Ticker(t).info['open'], yf.Ticker(t).info['currentPrice'], yf.Ticker(t).info['volume']
print(stocks)
This will create a tuple! --> since there are no bracktes, the return valule will consist of parenthsies 

stocks = {} #{'NVDA': [open, currentPrice, volume]}
for t in tickers:
    stocks[t] = [yf.Ticker(t).info['open'], yf.Ticker(t).info['currentPrice'], yf.Ticker(t).info['volume']]
print(stocks)

This will create a list! Adding the brackets will use the brackets in the return value making it a list

To label them we can add keys in the for loop. 

TUPLES ARE IMMUTABLE 
LISTS ARE MUTBLE

to see certain parts of a string in tuples:

tel = '123-456-7890'

a,b,c = tel.split('-')

c
will return = '7890'

*_, ext = tel.split('-')

ext
'7890'

phonebook = ['123-456-7890', '1-235-567-3425']

for p in phonebook:
    *_, last_four = p.split('-')

A fourth data dictionary is Sets

A set holds unique items, unordered: 
Sets are mutable 

watchlist = {'AAPL', 'NVDA', 'AAPL', 'MSFT'}
print(watchlist)   # {'AAPL', 'NVDA', 'MSFT'}

tickers = ['AAPL', 'NVDA', 'AAPL', 'MSFT', 'NVDA']
unique = set(tickers)    # {'AAPL', 'NVDA', 'MSFT'}
len(set(tickers))        # 3 unique stocks


##SPEED

Lists and sets are designed differnetly in the system 

Search in sets are much faster than lists 

Choosing Data Strucures 

Type	Mutable?	Ordered?	Access by	Duplicates?
str	    No	        Yes	        index	    Yes
list	Yes	        Yes	        index	    Yes
tuple	No	        Yes     	index	    Yes
dict	Yes	        Yes     	key	        Keys: No
set	    Yes	        No          membership	No


VERY IMPORTANT FOR QUIZ 

When to Use What?
list: ordered collection, may have duplicates
    Student grades, stock price history
tuple: fixed data that shouldn't change
    GPS coordinates, returning multiple values
dict: look up values by a meaningful key
    Stock prices by ticker, word frequency counts
set: unique items, fast membership testing
    Unique words in a document, valid usernames

You're building a music app:

    Your favorites playlist (add, remove, reorder)
    All artists a user has ever listened to (no repeats)
    Song title → number of times played
    A single song record: ('Bohemian Rhapsody', 'Queen', 1975)
## Code/work I'm proud of (optional)

## Challenges I faced

## Questions